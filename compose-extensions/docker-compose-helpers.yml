networks:
  adumbra_network:
    external: false

services:
  service-common:
    networks:
      - adumbra_network
    volumes:
      - "../backend/adumbra/config:/workspace/config"
      - "../backend/adumbra/ia:/workspace/ia"
      - "../backend/adumbra/workers:/workspace/workers"
      - "../backend/adumbra/database:/workspace/database"
      - "../datasets:/datasets"
      - "../models:/models"
